# API Secret Key - REQUIRED: Change this to a secure random string
# Generate a secure key using: openssl rand -hex 32
API_SECRET_KEY=change-me-to-a-secure-random-string

# Backend Configuration
GIN_MODE=release

# Frontend Configuration
NODE_ENV=production
HOST=0.0.0.0
PORT=3000
FRONTEND_PORT=3001
BACKEND_URL=http://backend:8080
ALLOWED_ORIGIN=http://localhost:3001

# GeoIP Database Configuration (Optional)
# Download GeoLite2 databases from: https://dev.maxmind.com/geoip/geolite2-free-geolocation-data
# Extract the .mmdb files and place them in the ./geoip/ folder
GEOIP_DB_PATH=geoip/GeoLite2-City.mmdb
GEOIP_ASN_DB_PATH=geoip/GeoLite2-ASN.mmdb
